# requirements.txt
# QUANT EDGE — Python dependencies
# Install: pip install -r requirements.txt
# Python >= 3.10 required

# ── Core ML ───────────────────────────────────────────────────
xgboost>=2.0.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.12.0
hmmlearn>=0.3.0
shap>=0.44.0

# ── Data ──────────────────────────────────────────────────────
yfinance>=0.2.36
requests>=2.31.0
feedparser>=6.0.11
beautifulsoup4>=4.12.0
lxml>=5.1.0
pytrends>=4.9.2

# ── Angel One SmartAPI (real-time price feed) ─────────────────
smartapi-python>=1.5.5
pyotp>=2.9.0
logzero>=1.7.0

# ── NLP / AI ──────────────────────────────────────────────────
transformers>=4.38.0
torch>=2.2.0                  # CPU version sufficient
anthropic>=0.20.0

# ── Social ────────────────────────────────────────────────────
tweepy>=4.14.0
praw>=7.7.1

# ── Alerts ────────────────────────────────────────────────────
python-telegram-bot>=21.0.0

# ── Finance ───────────────────────────────────────────────────
ta>=0.11.0                    # technical indicators
# TA-Lib>=0.4.28              # optional, significantly better patterns
#                             # install separately: brew install ta-lib (Mac)
#                             #                     apt install libta-lib-dev (Linux)

# ── Scheduling ────────────────────────────────────────────────
schedule>=1.2.0
pytz>=2024.1

# ── Dashboard API ─────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0      # [standard] pulls uvloop + httptools
playwright>=1.42.0             # used by dashboard for chart screenshots
                               # post-install: python -m playwright install chromium

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0
joblib>=1.3.0
tqdm>=4.66.0

# ── Optional (for notebooks) ──────────────────────────────────
# jupyter>=1.0.0
# matplotlib>=3.8.0
# seaborn>=0.13.0
# plotly>=5.18.0